# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Item {
  id: ID!
  name: String!
  category: Category!
}

type Category {
  id: ID!
  name: String!
}

type Query {
  items: [Item!]!
  categories: [Category!]!
  item(id: ID!): Item
}