# GraphQL schema example
#
# https://gqlgen.com/getting-started/

scalar Time

type Item {
  id: ID!
  name: String!
  category: Category!
  createdAt: Time!
}

type Category {
  id: ID!
  name: String!
  createdAt: Time!
}

type Query {
  items: [Item!]!
  categories: [Category!]!
  item(id: ID!): Item
}